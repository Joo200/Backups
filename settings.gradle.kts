rootProject.name = "Backups"

pluginManagement {
    repositories {
        maven {
            url = uri("https://repo.terraconia.de/artifactory/public/")
        }
        gradlePluginPortal()
    }
}
